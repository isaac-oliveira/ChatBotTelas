import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    btn: {
        justifyContent: 'center',
        alignItems: 'center',
    },

    btnIcon: {
        color: '#fff',
        fontSize: 32,
        textAlign: 'center',
    },
})