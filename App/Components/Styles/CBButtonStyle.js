import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    btn: {
        padding: 15,
        marginLeft: 15,
        marginRight: 15,
        backgroundColor: '#077',
        borderRadius: 5,
    },

    btnText: {
        color: '#fff',
        fontSize: 22,
        textAlign: 'center',
    },
})